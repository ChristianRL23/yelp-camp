import './Footer.scss';

const Footer = ({ style }) => {
  return (
    <footer style={style} className="footer">
      <img src="/images/Logo.svg" alt="Logo" />
    </footer>
  );
};

export default Footer;
