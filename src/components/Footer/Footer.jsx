import './Footer.scss';
import logo from './Logo.svg';

const Footer = () => {
  return (
    <footer className="footer">
      <img src={logo} alt="Logo" />
    </footer>
  );
};

export default Footer;
